{% do headTitle().append(translate("txt-admin")) %}
{% do headTitle().append(translate("txt-affiliation")) %}
{% do headTitle().append(translate("txt-edit-question-category-%s")|format(category)) %}

<h1>{{ translate("txt-edit-question-category-%s")|format(category) }}</h1>

{% include 'affiliation/partial/form/question/category' %}
