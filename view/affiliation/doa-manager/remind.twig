{% do headTitle().append(translate("txt-admin")) %}
{% do headTitle().append(translate("txt-remind-doa-%s-in-%s")|format(
affiliation.parseBranchedName(),
affiliation.project
)) %}

<h1 class="display-4">{{ translate("txt-remind-doa-%s-in-%s")|format(
    affiliation.parseBranchedName(),
    affiliation.project
    ) }}</h1>

{% do form.prepare() %}

{{ form().openTag(form)|raw }}


<div class="form-group row">
    <label class="col-md-3 form-control-label control-label">{{ translate("txt-project-name") }}</label>

    <div class="col-sm-9">
        <p class="form-control-static">{{ projectLink(affiliation.project,'view-admin', 'name') }}</p>
    </div>
</div>

<div class="form-group row">
    <label class="col-md-3 form-control-label control-label">{{ translate("txt-affiliation") }}</label>

    <div class="col-sm-9">
        <p class="form-control-static">{{ affiliationLink(affiliation,'view-community') }}</p>
    </div>
</div>

{{ ztbformelement(form.get('receiver')) }}
{{ ztbformelement(form.get('subject')) }}
{{ ztbformelement(form.get('deeplinkTarget')) }}
{{ ztbformelement(form.get('message')) }}

<div class="form-group row">
    <label class="col-md-3 form-control-label control-label">{{ translate("txt-sender") }}</label>

    <div class="col-sm-9">
        <p class="form-control-static">{{ webInfo.sender }} {{ webInfoLink(webInfo,'edit','icon') }}</p>
    </div>
</div>

<div class="form-group row">
    <div class="offset-md-3 col-sm-offset-3 col-sm-9">
        {{ ztbformelement(form.get('submit')) }}
        {{ ztbformelement(form.get('cancel')) }}
    </div>
</div>

{{ form().closeTag()|raw }}
