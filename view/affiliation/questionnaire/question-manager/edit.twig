{% do headTitle().append(translate("txt-admin")) %}
{% do headTitle().append(translate("txt-affiliation")) %}
{% do headTitle().append(translate("txt-edit-question-%s")|format(question)) %}

<h1>{{ translate("txt-edit-question-%s")|format(question) }}</h1>

{% include 'affiliation/partial/form/questionnaire/question' %}
